plugins {
    id 'java-library'
}

dependencies {

    implementation project(':audio-model')

    implementation 'org.springframework.boot:spring-boot-starter-web'
}

jar {
    archiveName "${project.name}.jar"
}

